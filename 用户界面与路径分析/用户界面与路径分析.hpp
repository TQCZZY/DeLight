1  __declspec(dllexport)void receive(std::vector<std::pair<int, int> >Re);
2  __declspec(dllexport)std::vector<std::pair<int, int> > Sentpoints(HINSTANCE hInstance, int       nCmdShow);
#pragma comment(lib,"用户界面与路径分析.lib")